给一个已完成的网页游戏做视觉重做。**审美方向、配色、材质、版式全部由你决定，我不设限。**

## 工作方式

1. 源项目在 `~/Documents/lucky-coin/source/index.html`。**不要改动原目录。**
2. 新建 `~/Documents/lucky-coin-kimi/`，复制过去在那里工作。
3. 交付：`~/Documents/lucky-coin-kimi/index.html`，单文件。

## 项目是什么

《幸运硬币》，一个当铺主题的老虎机 roguelike。单文件 1403 行 / 59KB。玩法、数值、动画逻辑全部已完成并调过平衡。

现在它能玩，但**长得很朴素**。你的任务是让它好看、精致、有质感。怎么做由你判断——推翻现有配色和主题也可以，只要你觉得那样更好。

## 唯一的硬性要求：不要改游戏逻辑

改 `<style>` 和为视觉服务的 DOM，随便你。但不要动 JS 的行为，不要动任何数值。

需要注意的是，**这个游戏的动画和状态是靠一批 class / id 钩子驱动的**，所以"不改逻辑"在实践中意味着这些接口不能破——改名、删除、或改变嵌套导致选择器失效，游戏会看起来没坏但实际全瘫（滚轮不转、结算不亮、拉杆失灵）。

动手前请自己把 JS 通读一遍，找出所有 `getElementById` / `classList` / `querySelector` 依赖的钩子，确认你的方案不会破坏它们。

有一处特别容易踩：**`#grid` 的直接子元素必须正好是 30 个 `.cell`，顺序不能变**（JS 用 `children[r*6+c]` 定位）。如果你想做列分隔之类的效果，别按列加包裹 div。

另外文件里有几个 class 目前**没有样式**（比如按稀有度拼出来的 `r-common` / `r-uncommon` / `r-rare` / `r-legend`，以及 `#notches` 里的 `<i>`）。那不是遗漏，是留给你发挥的位置。

## 环境事实（不是设计限制，是这个页面的运行条件）

- 页面以**片段**形式发布，不要写 `<!DOCTYPE>` / `<html>` / `<head>` / `<body>`
- 运行在**严格 CSP** 下，**一切外部请求都会被拦掉**：字体 CDN、外部样式表、远程图片、fetch 全部不行。素材只能是内联 SVG / CSS / canvas / `data:` URI。（尤其注意别习惯性写 Google Fonts —— 那会静默回退，你看到的效果和实际发布的不一样。）
- 中文字体没法内联（CJK 动辄几 MB），只能用系统字体栈
- 单文件、原生 CSS，不要引框架或构建工具

## 交付时请一并说明

你为整体方向、配色、字体、材质做了哪些取舍，以及为什么这样选。
